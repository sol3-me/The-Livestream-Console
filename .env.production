# Production environment config — committed to git (no secrets here).
# NEXTAUTH_URL is set here so it is baked into the Docker image.
# The server's .env.local should contain only: GOOGLE_CLIENT_ID,
# GOOGLE_CLIENT_SECRET, and NEXTAUTH_SECRET — NOT NEXTAUTH_URL.

NEXTAUTH_URL=https://livestream.sol3.me
